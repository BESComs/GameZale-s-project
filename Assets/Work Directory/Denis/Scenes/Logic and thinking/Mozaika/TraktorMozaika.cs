using UnityEngine;

namespace Work_Directory.Denis.Scenes.Logic_and_thinking.Mozaika
{
    public class TraktorMozaika : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
